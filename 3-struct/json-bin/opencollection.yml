opencollection: 1.0.0

info:
  name: json-bin
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  headers:
    - name: X-Master-Key
      value: "{{key}}"
    - name: Content-Type
      value: application/json
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
